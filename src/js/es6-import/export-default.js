export default function () {
    console.log('foo')
}

/*

// 或
function foo() {
    console.log('foo')
}
export default foo

*/

// 输出默认只能 后面对应一个值 default相当于一个变量
// export default命令只能使用一次。所以，import命令后面才不用加大括号
